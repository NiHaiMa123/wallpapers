# Runtime artifacts and evidence (large, per-run, never tracked)
outputs/
artifacts/
reports/
logs/
inputs/
tmp/
keqinglog/

# Machine-local config (tracked template: *.example.yaml)
config/*.local.yaml

# Local-only copies that intentionally do not belong to the repo
MiniMax-H3-Skills-网盘版/
动态壁纸工作流/
backups/
vendor_audit/
legacy/
.agents/

# Caches and transient files
__pycache__/
*.pyc
*.log

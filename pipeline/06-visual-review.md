# Deprecated：旧版视觉验收阶段

本文件属于旧版状态机，**不再定义当前生产流程**。

当前视觉验收分别发生在：

- `02-t2i-and-image-selection.md`：静态图初筛 + Gate A；
- `03-motion-and-seed-screen.md`：低画质 seed 初筛 + Gate B；
- `04-native-1080p-take.md`：1080p take 重审 + Gate C；
- `05-frame-selection.md`：完整帧列 + Gate D；
- `07-final-qc-and-delivery.md`：最终成片 QC。

特别注意：全帧导出和用户留删现在只在 Gate D 触发，不再作为每个候选的通用强制步骤。
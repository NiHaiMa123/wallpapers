# Deprecated：旧版交付与报告阶段

本文件属于旧版状态机，**不再定义当前生产流程**。

当前最终交付见：

- `07-final-qc-and-delivery.md`

当前报告必须能追溯：

```text
Director Brief
-> selected image
-> selected seed
-> selected 1080p take
-> user keep list
-> interpolation
-> upscale
-> final QC
-> final output
```

不要把旧版 `draft/long_draft/final` 晋级记录当作当前生产流程的必填结构。